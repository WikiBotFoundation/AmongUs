int{
  mouse( x + y )
     d( mouse.controller)
 }
    int{
       control.mouse(impostor.controller)
      mouse( goToLocation = allLocation)
    }
       int{
        control( Location + control)
        }
